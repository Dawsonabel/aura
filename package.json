{
  "name": "aura-app",
  "version": "1.0.0",
  "private": true,
  "description": "Aura — faithful UI + real backend (Neon Postgres, phone-code auth, Apple IAP).",
  "main": "server.js",
  "scripts": {
    "build": "tsc",
    "prestart": "tsc",
    "pretest": "tsc",
    "start": "node server.js",
    "test": "node --env-file=.env --test --test-concurrency=1 test/*.test.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.2"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
