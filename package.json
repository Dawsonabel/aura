{
  "name": "gas-clone",
  "version": "1.0.0",
  "private": true,
  "description": "Gas app clone — faithful UI + real backend (Neon Postgres, phone-code auth, Apple IAP).",
  "main": "server.js",
  "scripts": {
    "build": "tsc",
    "prestart": "tsc",
    "start": "node server.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.2"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
