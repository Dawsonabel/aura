{
  "name": "aura-app",
  "version": "1.0.0",
  "private": true,
  "description": "Aura — GraphQL backend (apps/api, Cloudflare Workers) + TanStack Start frontend (apps/web).",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.10.9"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
